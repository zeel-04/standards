import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  preset: 'Preset (source of truth)',
  color: 'Color roles',
  spacing: 'Spacing',
  iconography: 'Iconography',
}

export default meta
